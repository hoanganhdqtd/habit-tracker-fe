import React from "react";

function DeleteConfirm({ isDeleteConfirm, setIsDeleteConfirm }) {
  const handleClose = () => setIsDeleteConfirm(false);
  return <div>DeleteConfirm</div>;
}

export default DeleteConfirm;
